# Base URL of the deployed Cloudflare Worker proxy (see proxy/README.md).
# Leave UNSET to run fully offline on local mock data (LocalDataSource).
# Set it to switch the app onto the live Notion schedule (NotionDataSource).
#
#   VITE_API_BASE=https://study-timer-proxy.<your-subdomain>.workers.dev
VITE_API_BASE=
